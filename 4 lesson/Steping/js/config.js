/**
 * объект с настройками игры
 * @property {int} rowsCount Колличество строк в карте.
 * @property {int} colsCount Колличество колонок в карте.
 */
const config = {
  rowsCount: 10,
  colsCount: 10
};